= DESCRIPTION:

Override the apache2.conf installed by RightScale web_apache cookbook.

= REQUIREMENTS:

= ATTRIBUTES: 

See metadata.rb 

= USAGE:

  * Clone this repo.
  * Clone the " (Chef)" template.
  * Clone the "RightScale Premium" repo path.
  * Add this cookbook to the top of the new repo path.
  * Change the repo path on the cloned templated.
  * Edit the apache2.conf.erb file.  This is the configuration that will be installed.  

